---
version: '3.8'
services:
  db:
    user: "!!UID!!"
    restart: unless-stopped
    ports:
      - "5444:5432"
    env_file:
      - ./.envs/.local/.postgres

  app:
    user: "!!UID!!"
    build:
      target: development-stage
      args:
        DJANGO_ENV: development
        USER_ID: "!!UID!!"
    env_file:
      - ./.envs/.local/.django
      - ./.envs/.local/.postgres
    stdin_open: true
    tty: true
